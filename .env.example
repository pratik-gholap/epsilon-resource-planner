# Epsilon Resource Planner - Complete Configuration Template
# This file documents ALL environment variables needed for full deployment
# Copy the relevant sections to your environment

# =============================================================================
# BACKEND CONFIGURATION (backend/.env)
# =============================================================================

# Database Connection (Oracle with SQLAlchemy)
DATABASE_URL=oracle+oracledb://admin:admin@127.0.0.1:1521/?service_name=orclpdb

# Backend Server Settings
BACKEND_HOST=127.0.0.1
BACKEND_PORT=8000

# CORS Allowed Origins (comma-separated)
ALLOWED_ORIGINS=http://localhost:5173,http://127.0.0.1:5173

# =============================================================================
# FRONTEND CONFIGURATION (frontend/.env)
# =============================================================================

# Backend API URL
VITE_API_BASE_URL=http://localhost:5000

# =============================================================================
# CLOUD DEPLOYMENT CONFIGURATION EXAMPLES
# =============================================================================

# Example 1: AWS EC2 / DigitalOcean / Similar Cloud VPS
# -----------------------------------------------------------------------------
# Backend (backend/.env):
#   DATABASE_URL=oracle+oracledb://epsilon_user:SecurePass123@rds-instance.region.amazonaws.com:1521/?service_name=ORCL
#   BACKEND_HOST=0.0.0.0
#   BACKEND_PORT=8000
#   ALLOWED_ORIGINS=https://epsilon-planner.com,https://www.epsilon-planner.com
#
# Frontend (frontend/.env):
#   VITE_API_BASE_URL=https://epsilon-planner.com/api
# -----------------------------------------------------------------------------

# Example 2: Separate Frontend/Backend Hosting
# -----------------------------------------------------------------------------
# Backend on backend.epsilon-planner.com (backend/.env):
#   DATABASE_URL=oracle+oracledb://user:pass@cloud-db.com:1521/?service_name=ORCL
#   BACKEND_HOST=0.0.0.0
#   BACKEND_PORT=8000
#   ALLOWED_ORIGINS=https://epsilon-planner.com,https://www.epsilon-planner.com
#
# Frontend on epsilon-planner.com (frontend/.env):
#   VITE_API_BASE_URL=https://backend.epsilon-planner.com
# -----------------------------------------------------------------------------

# =============================================================================
# DEPLOYMENT NOTES
# =============================================================================
# 1. Never commit .env files to version control
# 2. Keep passwords and sensitive data secure
# 3. Update ALLOWED_ORIGINS to match your frontend domain(s)
# 4. For Apache deployment, environment variables can be set in:
#    - Apache VirtualHost config (SetEnv directive)
#    - .env file loaded by the application
#    - System environment variables
# 5. Restart services after changing environment variables
