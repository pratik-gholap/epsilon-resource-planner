# Backend Configuration Template
# Copy this file to .env and update with your actual values

# =============================================================================
# DATABASE CONFIGURATION
# =============================================================================
# Oracle database connection string using SQLAlchemy format
# Format: oracle+oracledb://username:password@host:port/?service_name=service
# 
# Local Development Example:
# DATABASE_URL=oracle+oracledb://admin:admin@127.0.0.1:1521/?service_name=orclpdb
#
# Cloud Production Example:
# DATABASE_URL=oracle+oracledb://epsilon_user:your_password@your-cloud-db.com:1521/?service_name=ORCL
DATABASE_URL=oracle+oracledb://admin:admin@127.0.0.1:1521/?service_name=orclpdb

# =============================================================================
# SERVER CONFIGURATION
# =============================================================================
# Host and port for the Flask backend server
# For local development, use 127.0.0.1
# For production behind Apache/WSGI, this is typically not used (handled by WSGI)
# For standalone cloud deployment, use 0.0.0.0 to accept connections from all interfaces
BACKEND_HOST=127.0.0.1
BACKEND_PORT=8000

# =============================================================================
# CORS CONFIGURATION
# =============================================================================
# Comma-separated list of allowed origins for CORS
# Include all domains that will host your frontend
#
# Local Development Example:
# ALLOWED_ORIGINS=http://localhost:5173,http://127.0.0.1:5173
#
# Cloud Production Example:
# ALLOWED_ORIGINS=https://your-domain.com,https://www.your-domain.com
ALLOWED_ORIGINS=http://localhost:5173,http://127.0.0.1:5173

# =============================================================================
# NOTES
# =============================================================================
# - Never commit the actual .env file to version control
# - Keep sensitive credentials (passwords, API keys) secure
# - Update ALLOWED_ORIGINS to match your frontend domain in production
# - SQLite is NOT supported - Oracle database connection required
